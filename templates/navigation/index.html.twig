<div class="title-bar" data-responsive-toggle="responsive-menu" data-hide-for="medium">
    <button class="menu-icon" type="button" data-toggle="responsive-menu"></button>
    <div class="title-bar-title">Menu</div>
</div>

<div class="top-bar" id="responsive-menu">
    <div class="top-bar-left">
        <ul class="dropdown menu" data-dropdown-menu>
            {% for entry in entries %}
                <li{{ entry.active == true ? ' class="active"': '' }}>
                    {% if entry.children is defined %}
                        <a href="javascript:">{{ entry.title }}</a>
                        <ul class="menu">
                            {% for subEntry in entry.children %}
                                <li{{ subEntry.active ? ' class="active"': '' }}>
                                    {% if subEntry.children is defined %}
                                        <a href="javascript:">{{ subEntry.title }}</a>
                                        <ul class="menu">
                                            {% for subSubEntry in subEntry.children %}
                                                <li{{ subSubEntry.active ? ' class="active"': '' }}>
                                                    <a href="{{ subSubEntry.url }}">{{ subSubEntry.title }}</a>
                                                </li>
                                            {% endfor %}
                                        </ul>
                                    {% else %}
                                        <a href="{{ subEntry.url }}" class="dropdown-item">{{ subEntry.title }}</a>
                                    {% endif %}
                                </li>
                            {% endfor %}
                        </ul>
                    {% else %}
                        <a href="{{ entry.url }}">{{ entry.title }}</a>
                    {% endif %}
                </li>
            {% endfor %}
        </ul>
    </div>
</div>
